<h1>Lista de productos</h1>
<ul>
  {{#each products}}
  <li>
    <h2>{{this.title}}</h2>
    <p>{{this.description}}</p>
    <p>Precio: {{this.price}}</p>
    <img src="{{this.image}}" alt="{{this.title}}">
  </li>
  {{/each}}
</ul>

